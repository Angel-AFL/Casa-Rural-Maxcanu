<section id="habitaciones" class="bg-white py-16 text-stone-800">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-3xl md:text-4xl font-bold text-emerald-700 mb-8">
      Nuestras Habitaciones
    </h2>

    <div class="relative overflow-hidden">
      <div class="flex animate-scroll space-x-6">
        <img src="" alt="Habitación 1" class="w-80 h-60 rounded-xl object-cover shadow-md" />
        <img src="" alt="Habitación 2" class="w-80 h-60 rounded-xl object-cover shadow-md" />
        <img src="" alt="Habitación 3" class="w-80 h-60 rounded-xl object-cover shadow-md" />
        <img src="" alt="Habitación 4" class="w-80 h-60 rounded-xl object-cover shadow-md" />
      </div>
    </div>

    <p class="mt-6 text-lg max-w-2xl mx-auto leading-relaxed">
      Cada habitación tiene su encanto, con detalles únicos que combinan el confort moderno y la esencia tradicional yucateca.
    </p>
  </div>
</section>

<style>
  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
  .animate-scroll {
    display: flex;
    animation: scroll 20s linear infinite;
    width: max-content;
  }
</style>
