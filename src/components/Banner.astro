---
interface BannerProps {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props as BannerProps;
---

<section>
  <img
    class="md:h-[500px] lg:h-[600px] h-[400px] w-full object-contain"
    src={src}
    alt={alt}
  />
</section>
